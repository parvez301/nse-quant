# Cloud (linux/arm64) requirements. Mirrors requirements.txt but installs
# pyqlib from source — the PyPI wheel is x86_64-only (no manylinux_aarch64).
# Keep in sync with requirements.txt manually.

# Core quant stack — pyqlib from git to get ARM64 Linux build
git+https://github.com/microsoft/qlib.git@v0.9.7
lightgbm>=4.0.0
pandas>=2.0
numpy>=1.24,<2.0
pyarrow>=15.0
scipy>=1.10
scikit-learn>=1.3

# Data ingestion
yfinance>=0.2.40
requests>=2.31

# Used by safety.py / data_loader paths
matplotlib>=3.7
tqdm>=4.65

# AWS SDK for SNS notify path
boto3>=1.34
